<UserControl x:Class="TaskClientPC.UserControls.Users_UserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TaskClientPC.UserControls" xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="0,20,0,0" RenderTransformOrigin="0.59,0.471">
        <!-- Background Gradient -->
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#FFE5A323" Offset="0.0"/>
                <GradientStop Color="#FFFF7043" Offset="1.0"/>
            </LinearGradientBrush>
        </Grid.Background>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="500"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" 
                Background="White" 
                CornerRadius="8" 
                Margin="20,20,-70,20" 
                Padding="10">
            <ListView x:Name="usersListView"
                  Grid.Row="1" Grid.Column="0"
                  SelectionChanged="usersListView_SelectionChanged" 
                  ScrollViewer.CanContentScroll="True" FontSize="10" HorizontalAlignment="Left" Width="540">
                <ListView.View>
                    <GridView ScrollViewer.HorizontalScrollBarVisibility="Visible">
                        <GridViewColumn Header="Name" Width="300">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding firstname}" Margin="5,0,0,0"/>
                                        <TextBlock Text="{Binding lastname}"/>
                                    </StackPanel>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="Type" Width="Auto" DisplayMemberBinding="{Binding userType}"/>
                    </GridView>
                </ListView.View>
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="FontSize" Value="20" />
                        <Setter Property="Foreground" Value="Blue" />
                        <Setter Property="Margin" Value="5" />
                    </Style>
                </ListView.ItemContainerStyle>
            </ListView>
        </Border>

        <!-- Right Side -->
        <Border Grid.Column="1"
                x:Name="DataBorder"
                Visibility="Collapsed"
                Background="White" 
                CornerRadius="8" 
                Margin="110,68,10,108" 
                Padding="10">
            <Border.Resources>
                <Style TargetType="TextBlock">
                    <Setter Property="Margin" Value="20"></Setter>
                    <Setter Property="FontSize" Value="20"/>
                </Style>
            </Border.Resources>
            <Grid x:Name="DataGrid">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="0*"/>
                    <ColumnDefinition Width="13*"/>
                    <ColumnDefinition Width="67*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Vertical" Grid.ColumnSpan="3">
                    <Button Grid.ColumnSpan="3" Background="red" Content="DELETE" Height="30" Width="80" HorizontalAlignment="Right"></Button>
                    <StackPanel Orientation="Vertical">
                        <StackPanel Orientation="Horizontal" x:Name="NameSP">
                            <TextBlock Text="Full Name: "/>
                            <TextBlock Text="{Binding firstname}" x:Name="FirstNameBlock"/>
                            <TextBlock Text="{Binding lastname}" Margin="0,20,20,0" x:Name="LastNameBlock"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" x:Name="EmailSP">
                            <TextBlock Text="Email: "/>
                            <TextBlock Text="{Binding email}" x:Name="EmailBlock"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" x:Name="BirthDaySP">
                            <TextBlock Text="Birthday: "/>
                            <TextBlock Text="{Binding birthday}" x:Name="BirthdayBlock"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" x:Name="UsertypeSP">
                            <TextBlock Text="Type: "/>
                            <TextBlock Text="{Binding userType}" x:Name="UsertypeBlock"/>
                        </StackPanel>
                        <Button
                            Style="{StaticResource MaterialDesignRaisedDarkButton}"
                            Height="50"
                            Width="200"
                            Content="UPDATE"
                            Click="OpenUpdateWindow"
                            Background="Green"
                            materialDesign:ButtonAssist.CornerRadius="25"
                            ToolTip="MaterialDesignRaisedDarkButton with Round Corners">
                        </Button>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
